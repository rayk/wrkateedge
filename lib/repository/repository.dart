/// Contains the repository parts of the application that hides and handles
/// the data access to the persistence store. Each repository exposes the
/// top level domain types that the particular repository is responsible for.
///
///
/// {@category Repository}
library repository;

export 'types/types.dart';
