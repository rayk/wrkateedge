import 'package:flutter/material.dart';

/// Handles the rendering the
class CardView extends StatelessWidget {
  const CardView({super.key});

  @override
  Widget build(BuildContext context) => Card();
}
